package hProjekt.controller.actions;

import hProjekt.controller.PlayerController;

/**
 * Player action for confirming treasure cards.
 * <p>
 * This action does not perform any operation when executed.
 */
public class ConfirmTreasureCards implements PlayerAction {
    @Override
    public void execute(final PlayerController pc) {
        // This is intentionally left blank
    }
}
